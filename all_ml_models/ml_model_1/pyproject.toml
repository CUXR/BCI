[project]
name = "bci-data-collection"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "brainflow>=5.21.0",
    "numpy>=2.2.6",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
